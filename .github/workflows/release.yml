name: release
on:
  push:

jobs:
  release:
    uses: salesforcecli/github-workflows/.github/workflows/npmTagAndRelease.yml@sm/npm-publish
    with:
      dryrun: true
      ctc: false
      sign: false
      tag: latest
      githubRelease: false
      prerelease: false
    secrets: inherit
